def initialize(dictionary_file_name)
    @dictionary = Set.new(words)

    def adjacent_words(word)
        words = []
        new_current_words = ""

        word.each_with_index do |i|
            word[i] + word[i + 1]
        end 
    if i !word.length - 1
        words << word[i] + word[i + 1]
    end 
    words
end 

def run(source, target)
    source = @current_words
    source = @all_seen_words

    arr.each do |ele|
        ele.current_words
    self.current_words unless current_words.empty? 
    
    arr.each do |word|
        ele.new_current_words do |word|
            if word !@all_seen_words 
                skip
            else word << new_current_words

                


